# 训练过程统计与可视化实现方案

> 时间戳：2026-05-24

## Context

当前 train.py 的训练监控存在以下问题：
1. **TensorBoard tag 冲突 bug**：Dice 和 IoU 写入同一个 tag（`"info/model_val_{}_dice/iou"`），后写的 IoU 覆盖 Dice，TensorBoard 中实际只能看到 IoU 曲线
2. **日志过于冗余**：每个 batch 都打印 image/label/output 的统计信息（回归测试用的 `[BASELINE]` 日志），正式训练时产生巨大日志文件（20000 iterations 约 10 万行）
3. **无可视化图表**：TensorBoard 只记录标量，没有生成可离线查看的图表文件
4. **缺少训练汇总**：训练结束后没有汇总报告，需要手动从 log.txt 中提取信息

目标：在不修改模型算法代码的前提下，新增一个独立的训练统计模块，自动采集关键指标，在训练结束后（或定期）生成可视化图表和汇总报告。

---

## 设计原则

- **独立模块**：新建 `utils/train_stats.py`，train.py 只需 import 并在关键点调用少量接口
- **不修改模型代码**：`SerpMamba.py`、`MambaLayer` 等模型文件不动
- **不修改数据加载**：`dataset_registry.py`、`dataset.py` 不动
- **最小侵入 train.py**：只在训练循环中添加数据采集调用（不改控制流、不改算法逻辑）
- **不依赖额外工具**：使用 matplotlib 生成静态图表 PNG，不要求安装新依赖（matplotlib 已在 test.py 中 import）

---

## 采集的关键指标

| 指标 | 采集时机 | 理由 |
|------|----------|------|
| 训练 loss（总 loss / CE / dice 分量） | 每个 iteration | 核心训练信号，判断收敛/发散 |
| 学习率 | 每个 iteration | 确认 lr schedule 是否按预期衰减 |
| 验证 Dice / IoU | 每次验证（当前每 10 iter） | 核心性能指标，跟踪泛化能力 |
| 每 epoch 耗时 | 每个 epoch 结束 | 发现性能瓶颈（如数据加载过慢） |

### 为什么只采集这些

这些是训练过程中**已经计算好的值**（loss 分量在 `logging.info("[BASELINE] loss=...")` 中已计算，lr 在 lr schedule 中已计算，验证 Dice/IoU 在验证循环中已计算）。采集它们只需传入已有变量，**零额外计算开销**。

### 不采集的指标

| 排除项 | 排除理由 |
|--------|----------|
| 训练时逐像素精度/召回 | 增加计算开销，验证集已有 Dice/IoU 足够判断 |
| 特征图可视化 | 需修改模型代码插入 hook，影响训练速度 |
| 梯度和权重分布统计 | 开销大，非日常训练必需 |
| GPU 显存/利用率 | 与训练逻辑无关，外部工具（nvidia-smi）可查 |

---

## 输出内容

### 1. 可视化图表（4 张 PNG）

训练结束后自动保存到 `{snapshot_path}/training_plots/` 目录：

| 图表 | 内容 | 判断用途 |
|------|------|----------|
| `loss_curve.png` | 总 loss + CE + dice 分量 vs iteration | 收敛趋势、两分量是否平衡 |
| `lr_curve.png` | 学习率 vs iteration | 确认 lr schedule 正确衰减 |
| `val_metrics.png` | 验证 Dice + IoU vs iteration | 泛化能力、是否过拟合 |
| `training_summary.png` | 2×2 子图汇总上述三张 + epoch 耗时 | 一览全局 |

### 2. CSV 中间文件（`training_stats.csv`）

训练过程中每 1000 个 iteration 追加写入 `{snapshot_path}/training_stats.csv`：

```
type,iter,value1,value2,value3,value4
train,1,0.8523,0.7891,0.9155,0.0001
train,2,0.8412,0.7756,0.9068,0.0001
val,10,0.4523,0.3012,,
epoch,1,118.5,,,,
```

- `train` 行：total_loss, ce_loss, dice_loss, lr
- `val` 行：mean_dice, mean_iou
- `epoch` 行：耗时秒数

**作用**：崩溃保护 + 可直接用 Excel/文本编辑器查看训练趋势。

### 3. 文本汇总（`training_report.txt`）

训练结束后写入 `{snapshot_path}/training_report.txt`：

```
=== Training Report ===
Dataset: fundus
Total iterations: 20000
Total epochs: 42
Total time: 1h 23m 15s
Best val Dice: 0.8234 @ iter 15200
Best val IoU:  0.7012 @ iter 15200
Final lr: 0.000003
Avg epoch time: 118.5s
```

---

## 新建文件：`utils/train_stats.py`

### 类接口设计

```python
class TrainStats:
    """训练过程统计采集器，独立于训练循环逻辑。"""

    def __init__(self, snapshot_path: str):
        """初始化，创建输出目录 {snapshot_path}/training_plots/。"""

    def log_train_step(self, iter_num, total_loss, ce_loss, dice_loss, lr):
        """记录单个训练步的指标。追加到内部列表。"""

    def log_val_step(self, iter_num, mean_dice, mean_iou):
        """记录一次验证结果。追加到内部列表。"""

    def log_epoch_time(self, epoch_num, seconds):
        """记录一个 epoch 的耗时。"""

    def plot_all(self, dataset_name, total_iters, total_epochs, total_time_s):
        """训练结束后调用：生成所有图表 + 汇总报告。"""
```

### 为什么用类而非函数

- 整个训练过程需要累积数据（loss 列表、验证指标列表等），类的实例天然持有状态
- 调用方只需 `stats = TrainStats(path)` + 各 `log_*` 调用 + 最终 `plot_all`，接口清晰
- 不引入全局变量

### CSV 中间文件：低频磁盘持久化

**问题**：纯内存累积在训练崩溃（OOM、CUDA error、手动 kill）时丢失全部统计数据。

**方案**：在内存列表照常累积的同时，每 1000 个 iteration（与 checkpoint 保存周期对齐）将当前累积的数据追加写入 CSV 文件 `{snapshot_path}/training_stats.csv`。

```python
# log_train_step 内部逻辑：
self._train_rows.append((iter_num, total_loss, ce_loss, dice_loss, lr))
if iter_num % 1000 == 0:
    self._flush_csv()
```

CSV 列：

```
type,iter,value1,value2,value3,value4
train,1,0.8523,0.7891,0.9155,0.0001    # total_loss, ce_loss, dice_loss, lr
train,2,0.8412,0.7756,0.9068,0.0001
val,10,0.4523,0.3012,,                 # mean_dice, mean_iou
epoch,1,118.5,,,,                       # seconds
```

**选择 CSV 而非 JSON/二进制格式的理由**：
- 可用文本编辑器/Excel 直接查看，无需额外工具
- 追加写入只需 `open(..., 'a')` + 一行 `writerow`，I/O 开销可忽略
- 崩溃后可从 CSV 恢复数据重新生成图表（`plot_all` 支持从 CSV 读取）

**I/O 开销分析**：每 1000 步追加约 1000 行（~50KB），写入耗时 <1ms，对训练无影响。

---

## 修改 `train.py`

### 修改点 1：import 和初始化（约 3 行）

位置：`train.py` 顶部 import 区 + `train()` 函数开头

```python
from utils.train_stats import TrainStats

# 在 train() 函数内，model 创建之后：
stats = TrainStats(snapshot_path)
```

### 修改点 2：记录训练步（约 2 行）

位置：loss 计算之后（现有 `logging.info("[BASELINE] loss=...")` 那行之后）

```python
stats.log_train_step(iter_num, loss.item(),
    ce_loss(outputs, label_batch.long()).item(),
    losses.dice_loss(outputs_soft[:, 1, ...], label_batch).item(), lr_)
```

**理由**：CE 和 dice 分量已经在现有代码中计算（用于 logging），直接复用，零额外计算开销。

### 修改点 3：记录验证步（约 2 行）

位置：验证循环结束后，`performance = np.mean(metric_list)` 之后

```python
stats.log_val_step(iter_num, performance, performance2)
```

### 修改点 4：记录 epoch 耗时（约 3 行）

位置：epoch 循环开始和结束

```python
import time
# epoch 循环开头：
t_epoch_start = time.time()

# epoch 结束处（`if iter_num >= max_iterations: break` 之前）：
stats.log_epoch_time(epoch_num, time.time() - t_epoch_start)
```

### 修改点 5：训练结束生成报告（约 3 行）

位置：`writer.close()` 之前

```python
stats.plot_all(dataset_name=dataset_cfg["name"],
               total_iters=iter_num,
               total_epochs=epoch_num + 1,
               total_time_s=time.time() - t_train_start)
```

其中 `t_train_start` 需在 `train()` 函数开头记录：`t_train_start = time.time()`

### 修改点 6：降级 BASELINE 日志（1 行，将 info 改为 debug）

将每个 batch 的 `[BASELINE]` logging.info 改为 `logging.debug`：

```python
# Before:
logging.info(f"[BASELINE] image shape=...")
# After:
logging.debug(f"[BASELINE] image shape=...")
```

**理由**：这些日志是为回归测试添加的，验证完成后已无日常价值。降级为 debug 不删除代码，需要时改回 logging 级别即可恢复。当前 logging 级别设为 `INFO`，debug 日志不会输出，log.txt 恢复干净。

### 修改点 7：修复 TensorBoard tag 冲突（1 行 → 2 行）

```python
# Before (line 309):
writer.add_scalar("info/model_val_{}_dice/iou".format(class_i + 1), ...)

# After：
writer.add_scalar("info/model_val_{}_dice".format(class_i + 1), metric_list[class_i], iter_num)
writer.add_scalar("info/model_val_{}_iou".format(class_i + 1), metric_list2[class_i], iter_num)
```

**理由**：原代码 bug —— Dice 和 IoU 写入同一个 tag（`dice/iou` 含斜杠但 tensorboardX 不会将其视为分组），后写的 IoU 值覆盖前写的 Dice 值。修复后 Dice 和 IoU 各有独立 tag，TensorBoard 中可同时看到两条曲线。

### train.py 修改总结

| 改动 | 行数 | 必要性 | 说明 |
|------|------|--------|------|
| import TrainStats + 初始化 | 3 | 核心 | 启用统计模块 |
| log_train_step 调用 | 2 | 核心 | 采集训练 loss |
| log_val_step 调用 | 2 | 核心 | 采集验证指标 |
| log_epoch_time 调用 | 3 | 辅助 | 采集 epoch 耗时 |
| plot_all 调用 | 3 | 核心 | 生成图表和报告 |
| BASELINE 日志降级 | 1 | 清理 | 减少日志噪音 |
| TensorBoard tag 修复 | 1→2 | 修 bug | Dice/IoU 分开记录 |

总计约 **15 行新增/修改**，不影响训练循环的控制流和算法逻辑。

---

## 不做的事

- 不修改 `SerpMamba.py` 或任何模型代码
- 不修改 `dataset_registry.py` 或数据加载逻辑
- 不修改 `val_2D.py`（验证函数保持原样）
- 不添加实时仪表盘（TensorBoard 已有此功能）
- 不做特征图/注意力可视化（需要改模型代码，超出范围）
- 不做逐样本训练指标（开销大，验证集已覆盖）
- 不引入额外依赖（matplotlib 已在 test.py 中 import）
- 不做高频磁盘写入（CSV 每 1000 步追加一次，非每步写入）

---

## 现有 TensorBoard 使用说明

TensorBoard 是当前项目中**唯一的训练过程实时监控工具**，已在 `train.py` 中使用。

### 当前写入的内容

`train.py` 通过 `tensorboardX.SummaryWriter` 将数据写入 `{snapshot_path}/tf-logs/` 目录：

| 标量名 | 内容 | 写入频率 |
|--------|------|----------|
| `lr` | 当前学习率 | 每个 iteration |
| `loss/model_loss` | 训练总 loss（CE + dice） | 每个 iteration |
| `info/model_val_1_dice/iou` | 验证 Dice/IoU（存在 tag 冲突 bug） | 每 10 个 iteration |

### 如何查看

TensorBoard 提供本地 Web 界面实时查看训练曲线：

```bash
# 在 Serp_Mamba 目录下执行：
tensorboard --logdir=experiments/<数据集名>/tf-logs

# 例如 fundus 数据集：
tensorboard --logdir=experiments/fundus/tf-logs
```

然后在浏览器打开 `http://localhost:6006` 即可看到 lr、loss、验证指标随 iteration 变化的曲线。

### TensorBoard 与本方案的关系

| 对比项 | TensorBoard（已有） | TrainStats（本方案新增） |
|--------|---------------------|--------------------------|
| 查看方式 | 需启动 Web 服务，实时查看 | 静态 PNG，离线直接打开 |
| 数据完整性 | 依赖进程正常结束 flush | CSV 每 1000 步落盘，崩溃安全 |
| 指标粒度 | 总 loss、lr、验证 Dice/IoU | 额外提供 CE/dice 分量、epoch 耗时 |
| 训练汇总 | 无 | 自动生成 training_report.txt |

两者互补：TensorBoard 用于训练**过程中**实时观察，TrainStats 用于训练**结束后**离线回顾和报告。修复 TensorBoard 的 tag 冲突 bug 后，Dice 和 IoU 曲线将各自独立显示。

---

## 验证方案

1. 用 fundus 数据集跑 `python train.py --dataset fundus --max_iterations 50`
2. 检查 `{snapshot_path}/training_plots/` 下生成 4 张 PNG，图表内容合理
3. 检查 `{snapshot_path}/training_report.txt` 内容正确（数据集名、iterations 数、best Dice 等数值正确）
4. 检查 TensorBoard 中 Dice 和 IoU 为独立曲线（修复 tag 冲突）
5. 检查 `log.txt` 不再有逐 batch 的 BASELINE 日志
