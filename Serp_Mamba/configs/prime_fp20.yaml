# PRIME-FP20 UWF-SLO 视网膜血管分割数据集配置
# 复现 BaseDataSets 对 PRIME-FP20 的硬编码行为

name: "PRIME-FP20"
description: "PRIME-FP20 UWF-SLO retinal vessel segmentation dataset"

# 各 split 的数据根目录（train/val 和 test 在不同路径下）
root_dir:
  train: "datasets/PRIME-FP20_DataPort/PRIME-FP20-after-VAL1"
  val: "datasets/PRIME-FP20_DataPort/PRIME-FP20-after-VAL1"
  test: "datasets/PRIME-FP20_DataPort/PRIME-FP20-TEST/val1_test"

# split 名到实际目录名的映射
# test 路径 val1_test 下的目录名为 val_image/val_label
split_map:
  train: "training"
  val: "val"
  test: "val"

# 目录布局：{split} 由 split_map 映射后替换
image_dir: "{split}_image"
label_dir: "{split}_label"

# 文件匹配
image_ext: ".tif"
label_ext: ".png"

# 标签文件名推导（从图像文件名推导标签文件名）
label_name_transform:
  - replace: ["Img", "Label"]
  - replace: ["tif", "png"]

# 图像属性
image_mode: "L"           # 灰度加载
input_channels: 1         # 模型输入通道数
label_threshold: 0        # label[label > 0] = 1
label_rgb_mode: "mean"    # RGB 标签转单通道方式

# 训练默认超参（可被 argparse 覆盖）
patch_size: [1024, 1024]
batch_size: 1
max_iterations: 16000
base_lr: 0.0001
num_classes: 2
